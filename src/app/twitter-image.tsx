// Twitter card image reuses the Open Graph image (spec §13.5).
// Next.js auto-wires this file into twitter:image for every route.
export { default, alt, size, contentType } from "./opengraph-image";
